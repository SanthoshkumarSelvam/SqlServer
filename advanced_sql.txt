--01. Find employees who earn more than 50,000 and have a salary value.

select * from EMPLOYEES where SALARY > 20000;

--02. Show employee names in A–Z order. If two names are the same, show the higher salary first.

select FIRST_NAME, salary from EMPLOYEES ORDER BY FIRST_NAME ASC , SALARY desc;

--03. Show only different department numbers and ignore NULL values.

select DISTINCT DEPARTMENT_ID from EMPLOYEES where DEPARTMENT_ID is not null; 

--04. Find employees whose salary is below 25,000 or above 75,000, but ignore NULL salaries.

select * from EMPLOYEES WHERE SALARY < 10000 or SALARY > 15000 and DEPARTMENT_ID is not null;

--05. Find employees from departments 10, 20, or 30 whose salary is between 30,000 and 60,000.

SELECT * FROM EMPLOYEES where DEPARTMENT_ID in (10,20,30) and SALARY BETWEEN 10000 and 20000;

--06. Find employees whose name starts with S and has A somewhere after the first letter.

select * from EMPLOYEES where FIRST_NAME like 'Sa%';

--07. Find employees whose name has exactly 6 characters and whose third character is A.

select * from EMPLOYEES where length(FIRST_NAME)=6 and FIRST_NAME like '__a%';

--08. Show the first 3 and last 3 characters of each employee's name.

select SUBSTR(FIRST_NAME,1,3)|| '-'|| SUBSTR(FIRST_NAME,-3) from employees;

--09. Find the position of the second A in each employee's name.

select FIRST_NAME, INSTR(FIRST_NAME,'a', 1, 2) from EMPLOYEES;

--10. Remove spaces from employee names and display them in uppercase.

SELECT upper(TRIM(FIRST_NAME)) FROM EMPLOYEES;

--11. Show first name and last name as one name, with first name in uppercase and last name in lowercase.

select upper(FIRST_NAME)||'-'||lower(LAST_NAME) from EMPLOYEES;

--12. Show salary rounded to 2 decimal places and also show the remainder after dividing salary by 1000.

select SALARY, ROUND(SALARY,2) from EMPLOYEES WHERE MOD(SALARY,1000)=0;

select SALARY, ROUND(SALARY,2),MOD(SALARY,1000) from EMPLOYEES;

--13. Show salary without decimal values and without rounding.

select trunc(SALARY) from employees;

--14. Find employees whose salary can be divided by 1000 without a remainder.

select * from EMPLOYEES where mod(SALARY,1000)=0;

--15. Find employees whose salary cannot be divided by 1000 and is greater than 30,000.

select * from EMPLOYEES where mod(SALARY,1000)!=0 and SALARY > 10000;

--16. Show employees who have commission and add % after the commission value.

select * from EMPLOYEES;

select FIRST_NAME, COMMISSION_PCT||'%' as percentage from EMPLOYEES where COMMISSION_PCT is not null;

--17. Show commission as No Commission when it is NULL; otherwise add % after the value.

select nvl(TO_CHAR(COMMISSION_PCT)||'%','NO_Commission') from EMPLOYEES;

SELECT FIRST_NAME,
       NVL(TO_CHAR(COMMISSION_PCT) || '%', 'No Commission') AS COMMISSION
FROM EMPLOYEES;

--18. Find employees whose commission is NULL or salary is below 30,000, but don't show department 10 employees.

select * from EMPLOYEES where COMMISSION_PCT is null and SALARY < 20000 and DEPARTMENT_ID not in(10);

--19. Find employees whose name contains the letter A exactly two times.

--20. Find employees whose name starts with A or S, has more than 4 characters, and salary is above 25,000.

select * from EMPLOYEES where FIRST_NAME like 'S%' and length(FIRST_NAME)>4;

--21. Show employees by department number from low to high and salary from high to low. Put NULL salaries at the end.

select DEPARTMENT_ID, SALARY from EMPLOYEES ORDER BY DEPARTMENT_ID, SALARY desc nulls last;

--22. Show employees by salary from high to low. Put NULL salaries at the beginning.

select DEPARTMENT_ID, FIRST_NAME, SALARY from EMPLOYEES ORDER BY SALARY desc nulls first;

--23. Find employees whose names have unwanted spaces at the beginning or end.

select FIRST_NAME, length(FIRST_NAME) from EMPLOYEES;

SELECT *
FROM EMPLOYEES
WHERE FIRST_NAME != TRIM(FIRST_NAME);

--24. Find employees whose name, after removing spaces and converting to uppercase, starts with A.

select upper(trim(FIRST_NAME)) from employees where FIRST_NAME like 'A%';

--25. Show employee name, name length, position of A, and first 3 characters together.

select FIRST_NAME, LENGTH(first_name) as length, instr(FIRST_NAME,'A',1,1) position ,
SUBSTR(FIRST_NAME,1,3) portion from EMPLOYEES;

--26. Display salary like 50,000.00 using TO_CHAR().

select TO_CHAR(500000, '99,99,999') from EMPLOYEES;

--27. Display the employee hire date like 24-AUG-2026 using TO_CHAR().

select * from EMPLOYEES where HIRE_DATE = '24-AUG-2026';

select FIRST_NAME from EMPLOYEES where TO_CHAR(HIRE_DATE, 'DD-MON-YYYY')='24-AUG-2026';

--28. The company wants a unique employee ID, mandatory employee name, unique email, 
--and salary greater than 0. Which constraints are needed?

create table employee(emp_id number constraint emp_id_uq unique,
emp_name varchar(15) constraint emp_name_nn not null,
email varchar(15) constraint email_nn not null,
salary number constraint sal_ck check(salary > 0);

--29. An existing table needs to stop duplicate emails and negative salaries. Which constraints should be added?

unique and check constraint

--30. An employee salary is updated, then another salary is updated, and then a record is deleted. 
--Keep the first update but cancel the second update and deletion. Use SAVEPOINT, ROLLBACK, and COMMIT.

update employee set column salary = 25000;

update employee set column salary = 25000 where emp_id = 101;

delete from employee where emp_id = 101;


--Another 50 Questions

--01. Display the first name, last name, and salary of employees whose salary is greater than 8000 and less than 20000.

select FIRST_NAME, LAST_NAME, SALARY from EMPLOYEES
where SALARY > 8000 and SALARY < 20000;

select FIRST_NAME, LAST_NAME, SALARY from EMPLOYEES
where SALARY BETWEEN 8000 and 20000;

--02. Display employee names and their annual salary after adding a 15% salary increment.

select FIRST_NAME, SALARY, SALARY * 1.15 as incremented_sal from EMPLOYEES; 

--03. Display employee names, salary, and salary after deducting 10% from their current salary.

select FIRST_NAME, SALARY, SALARY * 0.90 as deduct_sal from employees;

--04. Display employee names and calculate their salary after a 25% increment using arithmetic operators.

select FIRST_NAME, SALARY, SALARY *1.25 as incremented_sal from employees;

--05. Display employees whose salary is divisible by 1000 using the MOD() function.

select FIRST_NAME, SALARY from employees where mod(salary,1000)=0;

--06. Display employees whose salary is not divisible by 1000.

select FIRST_NAME, SALARY from EMPLOYEES where mod(SALARY,1000)!=0;

--07. Display employee names and salary rounded to the nearest thousand using ROUND().

select FIRST_NAME, SALARY , ROund(SALARY, -3) as rounded_sal from EMPLOYEES;

--08. Display employee names and salary truncated to the nearest thousand using TRUNC().

select FIRST_NAME, SALARY, trunc(SALARY, -3) truncated_sal from EMPLOYEES;

--09. Display employees whose salary rounded to the nearest thousand is greater than 10000.

select FIRST_NAME, SALARY, ROUND(SALARY, -3) from EMPLOYEES WHERE SALARY > 10000;

--10. Display employee names along with their full name by concatenating FIRST_NAME and LAST_NAME.

select FIRST_NAME||'-'||LAST_NAME from EMPLOYEES;

--11. Display employee names in the format: Steven King - AD_PRES.

select * from EMPLOYEES;

select FIRST_NAME||'-'||JOB_ID from EMPLOYEES;

--12. Display employee details where the first name is displayed in uppercase and last name in lowercase.

select UPPER(FIRST_NAME), lower(LAST_NAME) from EMPLOYEES;

--13. Display the first name and the number of characters in each employee's first name.

select first_name, length(FIRST_NAME) as num_char from EMPLOYEES;

--14. Display employees whose first name contains more than 5 characters.

select EMPLOYEE_ID, FIRST_NAME from EMPLOYEES where length(FIRST_NAME)>5;

--15. Display employees whose first name contains exactly 5 characters using LENGTH().

select EMPLOYEE_ID, FIRST_NAME from EMPLOYEES where length(FIRST_NAME)=5;

--16. Display the first 3 characters of every employee's first name using SUBSTR().

select FIRST_NAME, SUBSTR(FIRST_NAME,1,3) from EMPLOYEES;

--17. Display the last 3 characters of every employee's last name using SUBSTR().

select FIRST_NAME, SUBSTR(FIRST_NAME, -3) from EMPLOYEES;

--18. Display employees whose first name starts with the letter A using a string function.

SELECT * FROM EMPLOYEES WHERE FIRST_NAME like 'A%';

select * from EMPLOYEES where substr(FIRST_NAME,1,1)='A';

--19. Display employees whose first name ends with the letter N.

select * from EMPLOYEES where FIRST_NAME like '%n';

select * from EMPLOYEES where substr(FIRST_NAME,-1)='n';

--20. Display employees whose first name contains the substring 'an'.

select * from EMPLOYEES where FIRST_NAME like '%an%';

--21. Display employees whose last name contains the letter 'a' in the second position.

select * from EMPLOYEES where LAST_NAME like '_a%';

select LAST_NAME from EMPLOYEES where INSTR(LAST_NAME,'a',2,1)=2;

--22. Display employee names after removing leading and trailing spaces from their names using TRIM().

select FIRST_NAME, trim(FIRST_NAME) from EMPLOYEES;

--23. Display employee names in uppercase along with their email IDs in lowercase.

select FIRST_NAME, UPPER(FIRST_NAME) upper_case, EMAIL, lower(EMAIL) lower_case from EMPLOYEES;

--24. Display employee names and hire dates in the format DD-MON-YYYY using TO_CHAR().

select FIRST_NAME, HIRE_DATE, TO_CHAR(HIRE_DATE, 'DD-MON-YYYY') other_format from EMPLOYEES;

--25. Display employee names and hire dates in the format YYYY/MM/DD using TO_CHAR().

select FIRST_NAME, HIRE_DATE, TO_CHAR(HIRE_DATE, 'YYYY/MM/DD') other_format from EMPLOYEES;

--26. Display employee names and salary in the format 'Salary: 24,000' using TO_CHAR().

select FIRST_NAME||' - Salary:'||to_char(SALARY,'99,999') from EMPLOYEES;

--27. Display employee names and commission percentage, replacing NULL commission values with 0.

select FIRST_NAME, NVL(commission_pct,0) from EMPLOYEES;

--28. Display employee names and manager IDs, displaying 'Top Level Manager' when MANAGER_ID is NULL.

select FIRST_NAME, nvl(to_char(MANAGER_ID), 'Top level Manager')from EMPLOYEES;

--29. Display employees who have a manager and whose manager ID is greater than 100.

select FIRST_NAME, MANAGER_ID from EMPLOYEES where MANAGER_ID > 100;

--30. Display employees who do not have a manager using IS NULL.

select FIRST_NAME, nvl(MANAGER_ID, 0) from EMPLOYEES WHERE MANAGER_ID is null;

--31. Display employees who have a commission percentage using IS NOT NULL.

select * from EMPLOYEES where COMMISSION_PCT is not null;

--32. Display employees whose salary is between 5000 and 15000 but exclude salary 9000.

select * from EMPLOYEES where SALARY BETWEEN 5000 and 15000 and SALARY != 9000;

--33. Display employees whose department ID is either 30, 50, or 90 using IN.

SELECT * FROM EMPLOYEES WHERE DEPARTMENT_ID in (30, 50, 90);

--34. Display employees whose department ID is not 30, 50, or 90 using NOT IN.

select * from EMPLOYEES where DEPARTMENT_ID not in (30, 50, 90);

--35. Display employees whose first name starts with S or A using LIKE and logical operators.

select * from EMPLOYEES where FIRST_NAME like'S%';

--36. Display employees whose first name contains exactly two occurrences of the letter 'a'.

select * from EMPLOYEES where FIRST_NAME like '%a%a%';

--37. Display employees whose first name has 'a' as the second character.

select * from EMPLOYEES where FIRST_NAME like '_a%';

--38. Display employees whose last name starts with K and salary is greater than 10000.

select * from EMPLOYEES where LAST_NAME like 'K%' and SALARY > 10000;

--39. Display employees who belong to department 60 and have a salary greater than 5000.

select * from EMPLOYEES where SALARY > 5000 and DEPARTMENT_ID = 60;

--40. Display employees who belong to department 30 or 50 and have salary less than 8000.

select * from EMPLOYEES where SALARY < 8000 and DEPARTMENT_ID in (30, 50);

--41. Display employees whose salary is greater than 5000 and either belong to department 30 or have job ID 'IT_PROG'.


select * from EMPLOYEES where SALARY > 5000 and (DEPARTMENT_ID = 30 or JOB_ID = 'IT_PROG');

--42. Display employees whose salary is between 5000 and 12000 and whose first name starts with A.

select * from EMPLOYEES where SALARY BETWEEN 5000 and 12000 and FIRST_NAME like 'A%';

--43. Display employees in department 60 whose salary is not between 5000 and 10000.

select * from EMPLOYEES where SALARY not between 5000 and 10000 and DEPARTMENT_ID = 60;

--44. Display employees whose first name is either Steven, Neena, or Lex.

select * from EMPLOYEES where FIRST_NAME in ('Steven', 'Neena', 'Lex');

--45. Display employees whose job ID starts with 'FI' and salary is greater than 7000.

select * from EMPLOYEES where JOB_ID like 'FI%' and SALARY > 7000;

--46. Display employee names and salary sorted by salary from highest to lowest, 
--with employee names sorted alphabetically when salaries are equal.

select * from EMPLOYEES ORDER BY SALARY desc, FIRST_NAME asc;

--47. Display employees sorted by department ID ascending and salary descending, while placing NULL manager IDs at the end.

select * FROM EMPLOYEES ORDER BY DEPARTMENT_ID asc,
SALARY desc, MANAGER_ID nulls last;

--48. Display employee names, salary, and hire date sorted by hire date from newest to oldest, placing NULL values last.

select FIRST_NAME, SALARY, HIRE_DATE from EMPLOYEES
ORDER BY HIRE_DATE desc, MANAGER_ID nulls last;

--49. Create a new table named EMPLOYEE_BACKUP containing the same columns and data as the EMPLOYEES table.

create table emp_backup(col_1

--50. Create a new table named EMP_TEST with EMPLOYEE_ID as the primary key, 
--FIRST_NAME and LAST_NAME as mandatory fields, SALARY greater than 0 using a constraint, 
--and DEPARTMENT_ID with a default value of 10.

